<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.myexperiment1.ui.viewmodeltester.SharedViewModel" />

    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.viewmodeltester.ProducerFragment">

        <EditText
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#ACCDDF"
            android:hint="Text required"

            android:text="@={viewModel.liveText,default= @string/hello_blank_fragment}" />

    </FrameLayout>
</layout>